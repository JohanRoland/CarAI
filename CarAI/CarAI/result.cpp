/*
result takes the profile of the current user/users and an input string.

The string is parsed with the interpreter modual to a list of commands.

The commands are matched with there corresponding moduals/functions from a list of modual/functions
specifyed in the settinfs file.

*/